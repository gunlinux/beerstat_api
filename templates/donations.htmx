<table class="simple bordered">
  <thead>
    <tr class=title>
      <th>Donator</th>
      <th>Value</th>
    </tr>
  </thead>
  <tbody>
    {% for d in donations %}
      <tr class="donation-item">
        <td class="donation-name user">{{ d.name }}</td>
        <td class="donation-value">{{ d.value }} ₽</td>
      </tr>
      {% else %}
      <tr class="donation-empty"><td>No donations yet.</td></tr>
      {% endfor %}

      <!-- tr and td tags-->
  </tbody>
  <tfoot>
    <tr>
      <td colspan="2"></td>
    </tr>
  </tfoot>
</table>
